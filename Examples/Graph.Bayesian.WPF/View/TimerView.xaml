<UserControl x:Class="Graph.Bayesian.WPF.View.TimerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Graph.Bayesian.WPF.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid >
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="5*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>

        <!-- Here we create our GraphArea layout container wrapped into the Zoombox for vast zooming capabilities.
             This is the standard approach for GraphX.-->

        <DockPanel  Grid.Column="0" Margin="3">
            <local:ModelView x:Name="ModelView"></local:ModelView>
        </DockPanel>

        <Grid Grid.Column="1" Margin="3">
            <local:ServiceView x:Name="ServiceView"></local:ServiceView>
        </Grid>

        <DockPanel  Grid.Column="2" Margin="3">
            <local:DetailsView x:Name="DetailsView"></local:DetailsView>
        </DockPanel>
    </Grid>
</UserControl>
