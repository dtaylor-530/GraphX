using Graph.Bayesian.WPF.Infrastructure;
using System;
using System.Collections.Generic;

namespace Graph.Bayesian.WPF.Vertices
{
    //public record ViewModelProperty(string Name, ;

    //public record ViewModelProperties(Guid Guid, IReadOnlyCollection<ViewModelProperty> Collection);

    //public record ViewModelPropertiesMessage(string From, string To, DateTime Sent, IReadOnlyCollection<ViewModelProperty> Collection) : Message(From, To, Sent, Collection);


    public interface IViewModel
    {
        //public string Save();

    }
}